# Deficit register

This is the evidence ledger for the improvement program. It complements
`KNOWN_LIMITATIONS.md`: that file records deliberate limitations; this file
tracks observed defects, control gaps, and architectural risks through
reproduction, agreement, implementation, and verification.

No gameplay fix moves past `PROPOSED` until the user and Codex explicitly mark
its bounded change package `LOCKED_IN`.

## Status model

`REPORTED` -> `REPRODUCED` -> `ROOT_CAUSE` -> `PROPOSED` -> `LOCKED_IN` ->
`IMPLEMENTED` -> `VERIFIED`

- `REPORTED`: observation received; not yet independently reproduced.
- `REPRODUCED`: repeatable evidence exists.
- `ROOT_CAUSE`: the responsible path is confirmed.
- `PROPOSED`: one or more bounded remedies and tradeoffs are documented.
- `LOCKED_IN`: scope, constraints, acceptance, and tests are jointly approved.
- `IMPLEMENTED`: approved change exists but has not passed its full exit gate.
- `VERIFIED`: approved acceptance checks and the full gate pass.
- `DEFERRED`: intentionally parked with a reason and revisit condition.

## Severity

- `BLOCKER`: prevents the mode or release from functioning.
- `S1`: data loss, security boundary failure, or major systemic correctness.
- `S2`: materially harms playability, clarity, or development confidence.
- `S3`: localized defect or quality problem with a practical workaround.
- `S4`: polish or low-impact improvement.

## Current evidence

| ID | Area | Severity | Status | Evidence | Expected / next lock |
|---|---|---:|---|---|---|
| NET-001 | NET | BLOCKER | VERIFIED | Browser online boot sent `hello` from the `ClientWorld` constructor while the socket was CONNECTING. Plan 5 makes sessions explicit, begins them only after `open`, generation-guards stale sockets, bounds reconnect, suppresses disconnected intent, and exposes visible lifecycle status. Focused lifecycle tests plus real Local/Good/Degraded/Severe browser runs pass. | Preserve D-027 and direct online-browser startup coverage. |
| PST-001 | PST/NET | BLOCKER before public release | VERIFIED | The failing Plan 6 reproduction proved unauthenticated `hello` reached the core and any valid `charId` could be claimed. Protocol v2 now gates the core behind scrypt account auth, rotating digest-only sessions, and independent character ownership checks. The 144-test gate, real WebSocket register/resume/replay smoke, persisted-secret inspection, and 1280/1920 browser sign-in checks pass. | Preserve D-028 and `QA-AUTH` for protocol, identity, storage, proxy, or browser-session changes. |
| SOC-001 | SOC/NET/PST | S2 | VERIFIED | All characters automatically entered one global `fellowship`, disconnect erased membership despite the documented persistence rule, and strangers inherited revive/scaling assumptions. Plan 7 adds explicit nearby invite/accept/decline/leave, five-member/exclusivity constraints, durable offline membership, private feedback, party-only revive and first-engage scaling, and save schema v3 migration away from the legacy group. Focused sim/server/save tests, restart/reconnect, and the two-client browser flow pass. | Preserve D-029 and `QA-SOC`; matchmaking, guilds, trade, and instancing remain separate locks. |
| SOC-002 | SOC/UX/NET | S2 | VERIFIED | The HUD had no chat entry, typed keys were not isolated from game input, and server chat events were discarded at the next tick before replication. Plan 7 adds a focused Enter/Send/Escape composer, control/whitespace sanitation, 200-code-point cap, per-client throttle, and same-space delivery. Browser QA then caught per-frame DOM teardown and overlapping controls; interaction-stable rendering and semantic buttons now pass two-client delivery plus 1280/1920 layout checks. | Re-run `QA-SOC` for HUD render cadence, input focus, chat/event, protocol, party, or snapshot changes. |
| CNT-001 | CNT/WRL/QLT | S2 | VERIFIED | The interior authoring model had only one cave exemplar and content additions lacked a dedicated cross-catalog sanity guard. Plan 8 adds Siltroot Burrow, an exact navigation test, four-space traversal, and a table-driven catalog suite that resolves numeric, reference, graph, dialogue, location, and veteran-ability invariants. | Every content-family change must extend or pass `tests/content_catalog.test.ts`; every authored route or placement change must pass `world:tour`. |
| PRG-001 | PRG/CNT | S3 | VERIFIED | The playable catalog stopped at four weapons, four armor records, and five perks, leaving equipment and progression visibly shallow. Plan 8 reaches eight weapons, twelve armor records, and fifteen perks; all new gear is merchant-backed, all perk chains use existing modifiers, and shop purchase plus derived-stat behavior pass. | New types, hooks, stats, combat formulas, and save fields still require a separate lock under D-030. |
| UX-001 | UX | S2 | VERIFIED | Plan 1 replaces the bottom-edge strip with a structured controls card, a readable collapsed reminder, and an `H` toggle. Direct 1280x720 and scaled-harness 1920x1080 browser checks keep the card inside 24-pixel safe margins without resource overlap. | Re-run `QA-OFF-BOOT` and `QA-OFF-MENUS` when controls, keybindings, or supported viewports change. |
| UX-002 | UX | S2 | VERIFIED | Health, stamina, and magicka now have visible labels, numeric current/maximum values, semantic meter attributes, and distinct fill patterns. The Plan 1 browser checks and `tests/hud.test.ts` pass. | Preserve text and non-color differentiation for future resource styling. |
| CMB-001 | CMB | S2 | VERIFIED | Attack commands submitted during an existing attack were discarded; blocking could overlap committed attack frames. `tests/combat_correctness.test.ts` reproduced both. The sim now buffers one active/recovery follow-up, allows block to cancel recovery only, and emits private authoritative rejection reasons. The Plan 2 timing/browser gate passes. | Preserve `COMBAT_CONTRACT.md` timing and rejection tests when adding attacks. |
| CMB-002 | CMB | S2 | VERIFIED | Melee ignored height and player swings damaged any actor, including party members. Blocking mitigated rear and source-less damage. Focused tests now pin a 1.5 m vertical envelope, hostile-only hits, a 120-degree frontal block arc, stamina cost, and unblockable pools/source-less effects. Browser evidence confirms mitigated frontal-block feedback. | Revisit only with a locked vertical-combat or PvP package. |
| CMB-003 | CMB | S2 | VERIFIED | Flamebolt crossed both the Duskhollow pillar and implicit room wall in deterministic reproductions. Projectiles now choose the earliest swept actor/world impact across terrain, room boundaries, and solid prop height/AABBs. Both focused reproductions and the full gate pass. | Preserve wall/prop tests when projectile shapes or collision geometry change. |
| CMB-004 | UX/AV | S2 | VERIFIED | The HUD had no target identity or authoritative hit/block/hurt confirmation; renderer telegraphs showed only a generic caster ring. Actor views now expose exact danger shapes; HUD/renderer show target health, phase poses, flashes, markers, vignette, and exact cone/pool/ring geometry. Direct 1280 and scaled 1920 evidence passes. | Re-run `QA-CMB-SMOKE` for combat view/event changes. |
| CMB-005 | AV | S3 | VERIFIED | The browser host maps filtered authoritative combat events to a rate-limited synthesized cue palette after user activation. Plan 9 routes it through the D-031 mixer; cue mapping, bus gain, settings, and browser log checks pass. | Human-review final assets, spatial placement, and production mix remain KL-5. |
| CMB-006 | CMB/QLT | S3 | VERIFIED | Weapon output previously had no repeatable comparison harness. `npm run combat:bench -- seconds=30` now reports hits, mitigated damage/DPS, resource endpoints/wait, and rejected inputs for dagger, iron/steel swords, bow, and Flamebolt; Plan 2 metrics are recorded in `ENCOUNTER_DESIGN.md`. | Retain the harness and compare results for future tuning changes. |
| CMB-007 | CMB/PRG | S2 | VERIFIED | Plan 8 added `ironbound_bow`, but the browser host hard-coded `hunting_bow` as the only ranged weapon and submitted melee intent for the new bow. Plan 9 resolves equipped attack kind from the authored item catalog; both bows and a melee control are pinned. | Every new weapon must dispatch by authored weapon type, never item id. |
| CMB-008 | CMB/NET/UX | S1 | VERIFIED | QA found player projectile spells always released horizontally along model facing, ignoring the center reticle's vertical trajectory. D-034 adds bounded pitch to protocol v4 and the fixed-tick player aim state; Flamebolt now releases along the normalized 3D reticle ray while the server retains collision, hit, and damage authority. Focused sim/protocol/server/HUD tests, network gates, and two-viewport browser casts pass. | Preserve `SPELL_RETICLE_AIM_CONTRACT.md`; reticle/camera, spell release, projectile, input, or protocol changes must rerun QA-AIM. |
| AV-001 | AV/UX | S2 | VERIFIED | The renderer displayed latest 30 Hz transforms directly, producing the KL-1 stepping deficit. Plan 9 adds read-only one-tick position/shortest-yaw interpolation with explicit first-view, space-transition, and >4 m snap boundaries; camera, terrain center, and caster telegraphs share the displayed transform. Focused tests, the full gate, and direct browser rendering pass. | Preserve D-031 and `QA-AV`; no interpolation state may enter the sim or protocol. |
| AV-002 | AV/UX | S3 | VERIFIED | Audio had combat tones but no routing, environmental bed, mute, or volume UI. Plan 9 adds user-gesture master/effects/ambience/music buses, deterministic space/time tonal beds, persistent bounded settings, accessible controls, and focus-safe Escape close. Values survive browser reload; 1280/1920 layout and warning/error checks pass. | Final audio assets, spatial sources, device selection, and production loudness remain KL-5. |
| WRL-001 | WRL | S2 | VERIFIED | Rotated props rendered with yaw but movement/navigation used yaw-less AABBs; exterior projectile boxes also began at world y=0 while meshes sat on terrain. Plan 3 uses one oriented footprint and terrain-relative vertical bound for actors, navigation, projectiles, and camera obstruction. Focused tests pin both formerly missing and phantom smithy collision. | Preserve D-025 when adding prop shapes; a non-rectangular collider family requires its own locked schema change. |
| WRL-002 | WRL | S2 | VERIFIED | The authored road point at Fenharrow ended inside the inn shell; the mine return target was on a procedurally over-steep patch; Maera, Brandvar, Eydris, and the ruin chest had obstructed anchors. The road/anchors and mine apron are corrected, deterministic placement repairs invalid runtime points, and `npm run world:tour` passes 17 routes plus 34 placements across all three spaces. | Run `world:tour` for terrain, prop, room, route, door, spawn, schedule, or container changes. |
| WRL-003 | WRL/AV | S2 | VERIFIED | Navigation sampled endpoints coarsely and could append an unreachable exact goal; interior rendering removed an entire wall edge when only a narrow corridor overlapped. Navigation now sweeps shared occupancy from exact start to exact goal, movement substeps, and room-union boundary tests/rendering retain only the intended openings. | Preserve route-edge and room-boundary tests when changing navigation step or interior authoring. |
| WRL-004 | WRL/NET | S3 | VERIFIED | Space changes retained attacks/block/sprint and the traversal program had no explicit swimming decision or multi-player cell/transition isolation test. Transitions now clear only the moving character's incompatible transient state; any-player activation and isolation are pinned; water is explicitly wade-only to 0.5 m. Plan 4 subsequently adds cross-space NPC schedules under D-026. | Swimming still requires a later locked package; retain multiplayer isolation tests. |
| AI-001 | AI | S2 | VERIFIED | Perception used only distance/cone/stealth, so actors detected targets through implicit walls; threat hysteresis could retain an unseen target while a valid rival was visible. Plan 4 adds shared-world eye-to-eye obstruction and a visible-candidate continuity rule; focused reproductions and the 118-test gate pass. | Preserve D-012/D-026 when perception shapes or target priorities change. |
| AI-002 | AI | S2 | VERIFIED | Group aggro/reset was limited to one spawner and only cleared direct boss summons/pools. Separate authored pulls could split; defeated members, descendant sources, and projectiles survived inconsistent reset paths. `encounterId` now owns pull, scaling, descendants, mechanics, return, and atomic reset; focused reset/wipe tests and the full gate pass. | New multi-spawner pulls must share one space and extend the ownership tests. |
| AI-003 | AI | S2 | VERIFIED | Supports could heal unrelated/healthy actors, summon casts had no living cap, and cooldowns paused throughout a cast. AI now requires a useful legal target, allied factions are explicit, Warden adds cap at four, and cooldowns advance during telegraph/recovery. Focused tests and Matron/Warden browser observation pass. | Re-run `ai:bench` and browser mechanic checks when ability rules or numbers change. |
| AI-004 | AI/WRL | S2 | VERIFIED | Cross-space schedules stayed in the old space, and a returning actor with a disconnected valid home could remain in `return` forever. Directed-door schedule routing plus inactive route collapse and a 90-blocked-tick home recovery pass focused observed/offscreen tests and the full gate. | Preserve KL-4’s offscreen abstraction; rerun schedule tests when doors/schedules change. |
| QLT-005 | QLT/AI | S3 | VERIFIED | The boss benchmark had one simple unnamed policy and could not show whether blocks, add priority, pool movement, and revives changed pressure. `npm run ai:bench` now compares named fixed-seed naïve/mechanics policies and reports damage, blocks, downs, revives, interrupts, phases, wipes, and kills. Current comparison and browser evidence are recorded in QA/encounter docs. | Keep both policies deterministic and compare results for later tuning. |
| FND-001 | FND | S2 risk | REPRODUCED | `src/sim/sim.ts` is 1,255 lines with 71 methods and coordinates spawning, player lifecycle, parties, tick phases, combat facades, interactions, sessions, and persistence. | No refactor without a demonstrated defect. Use `SIM_RESPONSIBILITY_MAP.md` when a later plan proposes a seam. |
| QLT-001 | QLT | S2 | VERIFIED | Browser QA previously had no repeatable baseline and KL-7 stated that browser inspection was unavailable. The offline/online procedure was completed at 1280x720 and recorded in `QA_BASELINE.md`. | Re-run the relevant browser scenarios for every visual, interaction, or online-browser change. |
| QLT-002 | QLT | S2 | VERIFIED | The sim import graph had no circular-dependency guard. The Plan 0 gate passes with a non-vacuous acyclic-graph assertion. | `tests/architecture.test.ts` must remain green and non-vacuous. |
| NET-002 | NET | S2 | VERIFIED | Snapshot growth had no budget tripwire. The Plan 0 gate passes with representative four-player mine snapshots peaking at 8,077 bytes. | `tests/server_net.test.ts` enforces `< 32,000` UTF-8 bytes. |
| QLT-003 | QLT/NET | S2 | VERIFIED | Plan 5 adds an ordered fixed-seed virtual-time matrix over real ClientWorld/ServerCore plus a matching real WebSocket browser relay. All four standard profiles report authority delay, correction, throughput, loss, disconnects, pending tail, and remote motion; tests enforce the locked envelopes. | Run `net:bench` for prediction, protocol, input, snapshot, or transport changes. |
| NET-003 | NET | S2 | VERIFIED | Plan 5's queued-input reproduction showed `ackSeq` advanced when input entered the queue, before an authoritative tick consumed it, causing clients to discard an unprocessed reconciliation tail under bursts. Server queues now retain sequence numbers and advance the cumulative ack only as ticks consume them. | Preserve the partial-queue acknowledgement test in `server_net.test.ts`. |
| QLT-004 | QLT | S3 | VERIFIED | The locked baseline reproduced five development-tool advisories (3 moderate, 1 high, 1 critical) in Vite 5/Vitest 2 while production remained clean. Plan 10 upgrades to Vite 8.2/Vitest 4.1, records the Node floor and D-032 checks, and produces zero findings in both full and production-only audits. Clean install, 173 tests, production/standalone builds, and dev-server module requests pass. | Preserve `TOOLCHAIN_SECURITY_CONTRACT.md`; new majors, Node-floor changes, runtime dependency changes, or audit exceptions require a new lock. |
| MOV-001 | MOV/NET | S2 | VERIFIED | QA observed A/D moving opposite the camera's visible horizontal direction. The sim and online predictor used the same incorrect yaw transform. D-033 replaces both with one tested camera-basis helper; pure, sim, and ClientWorld reproductions now pin screen-right at yaw zero to world `-X`. | Preserve `PLAYER_MOVEMENT_RECOVERY_CONTRACT.md`; rerun focused movement plus `net:bench` when input axes, camera yaw, prediction, or movement change. |
| MOV-002 | MOV/NET | S2 | VERIFIED | QA observed sprint motion continuing after stamina exhaustion, while online prediction applied sprint whenever Shift was held and ignored authoritative stamina/derived speed. D-033 shares sprint rules, replicates required movement state in protocol v3, restores stationary regeneration, walks below the 10% restart floor, and pins final-tick behavior in sim and ClientWorld tests. | Movement-resource or prediction changes must preserve the exhaustion, stationary, restart-floor, and replay cases. |
| WRL-005 | WRL/UX/NET | S2 | VERIFIED | QA found no living-player escape from invalid terrain. Game Settings now exposes server-authoritative safe-ground recovery through `IWorld`, using existing per-space recovery points without resource restoration or death penalties. A 30-second cooldown, incapacitation checks, active attack/block checks, and a 30 m same-space hostile guard prevent combat use. | Preserve private feedback and server-owned destination selection; broader fast travel or unstuck heuristics require another lock. |
| INV-001 | INV/UX/NET | S2 | VERIFIED | QA found equipped gear mixed into the general inventory with only an asterisk and no fixed slot presentation or direct unequip action. D-035 exposes all six authoritative equipment slots, keeps empty slots visible, separates equipped gear from carried items, and routes equip/unequip through `IWorld` and protocol v5. Focused sim/server/presentation tests, the 196-test gate, network matrix, real WebSocket smoke, and 1280/1920 browser interaction/layout checks pass. | Preserve `PLAYER_LOADOUT_CONTRACT.md` for inventory/equipment changes. |
| CMB-009 | CMB/UX/NET/PST | S2 | VERIFIED | Keys 1/2 were hard-coded in the browser host, and known spells had no loadout UI or durable equipped state. D-035 adds two unique, authoritative per-character spell slots; casting requires known+equipped state; protocol v5 replicates them; world v4 and character v2 migrations preserve legacy defaults; the HUD provides explicit known-spell assignment and a quickbar. Focused sim/save/server/presentation tests, the 196-test gate, network matrix, real WebSocket smoke, and browser reassignment pass. | Preserve `PLAYER_LOADOUT_CONTRACT.md`; expanded spellbooks/hotbars remain separate locks. |
| MAG-001 | PRG/CNT/UX/NET/PST | S2 | VERIFIED | QA found magic immediately granted to every new character and too thin to express schools or acquisition. D-042 starts fresh characters uninitiated, adds six authoritative spell primers, groups six spells into four original disciplines, adds working Stonebinding/Veilcraft effects and progression, preserves legacy knowledge/loadouts, and privately replicates school/learning state through protocol v7. The 222-test gate, full benchmark/network matrix, real WebSocket smoke, and direct study/equip/cast browser checks at 1280/1920 pass with clean logs. | Preserve `MAGIC_PROGRESSION_CONTRACT.md`; new spell kinds, acquisition systems, hotbars, or school progression must rerun QA-MAGIC and network gates. |
| CAM-001 | AV/UX/CMB | S2 | VERIFIED | QA observed the third-person player model obscuring the center reticle because the renderer looked directly at the player's eye. D-036 adds a collision-aware `0.9 m` right-shoulder boom while preserving the exact D-034 ray direction; extreme compression hides only the local body. Three focused reproductions, the 199-test gate, and direct 1280/1920 browser evidence pass with no overflow or warning/error logs. | Preserve `THIRD_PERSON_CAMERA_CONTRACT.md`; camera composition, collision, aim, or local-body visibility changes must rerun QA-CAM. |
| WRL-006 | WRL/AV | S2 | VERIFIED | QA captured buried/floating Fenharrow props and a building suspended over terrain. D-037 makes site pads follow road shaping, so every current Fenharrow structure footprint is exactly level across three tested seeds; foundations extend below grade and the well has a complete rim/shaft. Focused tests, 23-route/43-placement world tour, the 203-test gate, and 1280/1920 browser evidence pass. | Preserve `WORLD_STRUCTURE_PLACEMENT_CONTRACT.md`; terrain order, height sampling, structure geometry, or site changes must rerun QA-STRUCT and `world:tour`. |
| WRL-007 | WRL/AV | S2 | VERIFIED | QA captured the Fenharrow Hearth entrance detached from its shell. D-037 authors exterior entrances from parent-prop local transforms, validates resolved X/Z/yaw against drift, and renders inherited rotation. The Hearth frame is flush with its facade at both browser viewports while all door endpoints remain traversable. | Preserve prop-relative anchor validation and QA-STRUCT for door, facade, interaction, schedule, or traversal changes. |
| AI-005 | AI/WRL | S2 | VERIFIED | QA videos show NPCs repeatedly pressing into buildings and jittering indoors. D-038 requires exact line/A* reachability for schedule/return moves, blocks direct collision sliding, and falls back after 60 blocked ticks to the last reached safe schedule home. The disconnected-room reproduction, every authored schedule leg, Plan 4 recovery tests, world tour, AI benchmark, 206-test gate, and live inn shift pass. | Preserve `NPC_SCHEDULE_STABILITY_CONTRACT.md`; schedule, door graph, navigation, collision, activity streaming, or AI movement changes must rerun QA-NPC. |
| AV-003 | AV/AI | S3 | VERIFIED | Procedural posing treated any per-frame displacement over 5 mm as full locomotion, magnifying tiny corrections into a 6 cm bob and 0.4 rad arm swing. D-038 uses renderer-only `0.18/0.08 m/s` start/stop hysteresis. Focused dead-zone tests and before/settled inn evidence pass at 1280/1920 with clean logs. | Keep locomotion history presentation-only and rerun QA-NPC for interpolation or pose changes. |
| AV-004 | AV/INV/NET | S2 | VERIFIED | QA reported invisible equipped items and weak generic combat poses in both viewpoints. D-039 adds compact authoritative equipment to every `ActorView`, protocol v6 replicates remote loadouts, world and camera-local rigs render the matching item families, and sword/dagger, heavy, bow, spell, and block states receive distinct procedural poses. The 210-test gate, network matrix, multiplayer/world benchmarks, real WebSocket smoke, and direct 1280/1920 third-/first-person QA pass with clean logs. | Preserve `EQUIPMENT_PRESENTATION_CONTRACT.md`; equipment replication, attachment factories, combat posing, camera framing, or item families must rerun QA-GEAR and network gates. |
| UX-003 | UX/WRL | S2 | VERIFIED | QA reported no world map or player-facing navigation tool. D-040 adds `M` current-space maps, an authored Kaldwyn road/five-destination survey, exact interior floor plans, north/coordinate/player-heading presentation, and selectable straight-line HUD bearing/distance. Focused tests, all 23 routes/43 placements, the 214-test gate, and direct 1280/1920 exterior/interior/cue QA pass with clean logs. | Preserve `MAP_NAVIGATION_CONTRACT.md`; geography, interiors, doors, map projection, controls, or destination cues must rerun QA-MAP and `world:tour`. |
| AV-005 | AV | S2 | VERIFIED | QA reported rigid block characters and inaccurate action motion. D-041 replaces the six-block humanoid with a stable articulated torso/head/face/shoulder/forearm/hand/hip/knee rig, tapered/faceted detail, full-body gait/idle/aim/sneak posing, and phase-aware elbows; quadrupeds gain articulated diagonal gait, head, and tail. Gear follows hand chains and first-person framing remains clear. The 218-test gate, unchanged AI comparison, and direct 1280/1920 close/armored/viewmodel QA pass with clean logs. | Preserve `CHARACTER_PRESENTATION_CONTRACT.md`; rig hierarchy, attachment points, posing, archetypes, equipment, or interpolation must rerun QA-RIG and QA-GEAR. |

## Playtest intake template

Use one row per independently observable symptom.

| ID | Area | Reported behavior | Expected behavior | Reproduction | Severity | Status | Dependencies |
|---|---|---|---|---|---|---|---|
| _Example: PLY-001_ | _PLY_ | _What happened_ | _What should happen_ | _Mode, location, input sequence, frequency_ | _S1-S4_ | `REPORTED` | _WRL, UX, etc._ |

## Evidence rules

1. Record offline and online behavior separately.
2. Separate symptoms from suspected causes until the cause is confirmed.
3. Attach the smallest deterministic or browser scenario that reproduces it.
4. Do not close an item on a unit test alone when the deficit is visual,
   interaction-based, timing-sensitive, or network-sensitive.
5. `VERIFIED` requires the locked acceptance checks plus `npm run gate`.
